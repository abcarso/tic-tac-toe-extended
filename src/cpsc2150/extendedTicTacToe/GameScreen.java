package cpsc2150.extendedTicTacToe;

import cpsc2150.extendedTicTacToe.models.BoardPosition;
import cpsc2150.extendedTicTacToe.models.GameBoard;

/*
Project 2
Alaina Carson
Section: 001
*/

public class GameScreen {
    private char currentPlayer;
    private GameBoard gameBoard;
    private BoardPosition boardPosition;

    /**
     * Creates an instance of GameScreen
     *
     * @post [creates an instance of GameScreen with no games played yet
     */
    private GameScreen() {

    }


    /**
     * Creates an instance of main
     * @param args
     *
     * @post [program is run]
     */
    private void main(String[] args) {

    }


    /**
     * Print to the screen once a win condition is reached
     * @param gameBoard the instance of the gameBoard in play
     *
     * @post gameBoard = #gameBoard AND [print a message to the screen if a win condition is met]
     */
    private void winCondition(GameBoard gameBoard) {

    }


    /**
     * Asks the user if they want to play again
     *
     * @pre [game has completed]
     * @post [starts a new game if user chooses, otherwise end program]
     */
    private void playAgain() {

    }


    /**
     * Print the current game board to screen
     * @param gameBoard the instance of the gameBoard in play
     *
     * @post gameBoard = #gameBoard AND [prints the current gameBoard to screen]
     */
    private void printBoard(GameBoard gameBoard) {

    }


    /**
     * Iterates the current player
     * @param currentPlayer
     *
     * @pre [currentPlayer has completed their turn]
     * @post currentPlayer != #currentPlayer
     */
    private void playerTurn(char currentPlayer) {

    }
}
